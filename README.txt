How to run the app:

You will need STS and Maven installed
	
Run the login.sql to create the MySQL database and change the database connection settings in servlet.context.xml before testing the project.

Please import the project into STS and run the app using Apache 8.0 server with 	the following URL:

	http://localhost:8080/myapp/